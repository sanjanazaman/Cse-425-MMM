# Music Feature Clustering Project

This project implements feature extraction, dimensionality reduction, and clustering on a hybrid language music dataset (Bangla + English songs). The project is structured into easy,medium, and hard tasks. Each demonstrating different approaches for feature extraction and clustering.



